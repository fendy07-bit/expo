// Aplikasi Kecamatan Lengkap - React Native Web
// Menggunakan komponen Tailwind dan sistem modular
import React from "react";
import { Card, CardContent } from "@/components/ui/card";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { ScrollArea } from "@/components/ui/scroll-area";

export default function KecamatanApp() {
  return (
    <div className="p-4 space-y-4">
      <h1 className="text-2xl font-bold">Aplikasi Kecamatan Wonomerto</h1>
      <Tabs defaultValue="umum" className="w-full">
        <TabsList className="grid grid-cols-3">
          <TabsTrigger value="umum">Data Umum</TabsTrigger>
          <TabsTrigger value="layanan">Pelayanan</TabsTrigger>
          <TabsTrigger value="monitoring">Monitoring</TabsTrigger>
        </TabsList>

        {/* Tab Data Umum */}
        <TabsContent value="umum">
          <ScrollArea className="h-96">
            <Card className="mb-4">
              <CardContent>
                <h2 className="font-semibold">Profil Kecamatan</h2>
                <p>Menampilkan data profil, sejarah, batas wilayah, dan peta kecamatan.</p>
              </CardContent>
            </Card>
            <Card className="mb-4">
              <CardContent>
                <h2 className="font-semibold">Data Desa</h2>
                <p>Detail seluruh desa, dusun, perangkat desa, dan potensi masing-masing.</p>
              </CardContent>
            </Card>
          </ScrollArea>
        </TabsContent>

        {/* Tab Layanan */}
        <TabsContent value="layanan">
          <ScrollArea className="h-96">
            <Card className="mb-4">
              <CardContent>
                <h2 className="font-semibold">Surat Menyurat</h2>
                <Button className="mt-2">Ajukan Surat Online</Button>
              </CardContent>
            </Card>
            <Card className="mb-4">
              <CardContent>
                <h2 className="font-semibold">Absensi Non-ASN</h2>
                <Input placeholder="Masukkan NIK/ID Pegawai" />
                <Button className="mt-2">Absen Sekarang</Button>
              </CardContent>
            </Card>
          </ScrollArea>
        </TabsContent>

        {/* Tab Monitoring */}
        <TabsContent value="monitoring">
          <ScrollArea className="h-96">
            <Card className="mb-4">
              <CardContent>
                <h2 className="font-semibold">Monitoring Kegiatan</h2>
                <p>Laporan kegiatan harian, bulanan, dan program prioritas.</p>
              </CardContent>
            </Card>
            <Card className="mb-4">
              <CardContent>
                <h2 className="font-semibold">Grafik Realisasi</h2>
                <p>Visualisasi LRA, PBB, dan Stunting dalam bentuk grafik.</p>
              </CardContent>
            </Card>
          </ScrollArea>
        </TabsContent>
      </Tabs>
    </div>
  );
}
